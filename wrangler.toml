name = "apt"
main = "worker.js"
compatibility_date = "2024-09-21"

[assets]
directory = "./dists"

# Serve static files (generated by reprepro) directly from assets, only proxy .deb files through worker
